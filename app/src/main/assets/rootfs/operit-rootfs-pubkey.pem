-----BEGIN PUBLIC KEY-----
# PLACEHOLDER — a real Ed25519 public key has not been provisioned yet.
# The on-device bootstrap deliberately fails signature verification while
# this placeholder is present, surfacing a visible error rather than
# silently accepting an unsigned rootfs.
#
# To replace:
#  1. Generate a keypair (e.g. openssl genpkey -algorithm Ed25519 ...).
#  2. Add the private key as the ROOTFS_SIGNING_KEY repository secret.
#  3. Overwrite this file with the matching public key PEM.
#  4. Ship a new app build.
#
# See app/src/main/assets/rootfs/README.md.
-----END PUBLIC KEY-----
